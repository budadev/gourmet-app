{
  "version": "0.1.70",
  "releaseDate": "2025-11-07",
  "changes": [
    "Added sub-type support for all item types with configurable enable/disable toggle",
    "Sub-types are searchable and included in search index",
    "Added sub-type filter in filter panel with cascading behavior based on type selection",
    "Sub-type filter displays as 'Type → Sub-type' format (e.g., 'Wine → Red')",
    "Sub-type filter automatically updates when type filters change",
    "Improved toggle styling to match Places screen design"
  ]
}
